<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5 bg-light p-4" style="border-radius: 10px;">
      <% if (messages.error) { %>
      <div class="alert alert-primary" role="alert"><%= messages.error %></div>
      <% } %>
      <h1>Login Page</h1>
      <form action="/login" method="POST">
        <div class="d-flex flex-column gap-1">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" class="input-group" />
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            class="input-group"
          />
          <button type="submit" class="btn btn-primary mt-2">Submit</button>
          <a href="/register" class="btn btn-primary mt-2">Register</a>
        </div>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
  </body>
</html>
